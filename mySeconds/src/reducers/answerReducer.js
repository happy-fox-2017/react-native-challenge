const initState = {
  answer: [{text: '', answer: null, img: null, animating: true, modalVisible: false}]
}

export default (state=initState, action) => {
  console.log('--------------------', action.type);

  switch (action.type) {
    case "GET_ANSWER":
      const

      break;
    default:
      
  }
}
